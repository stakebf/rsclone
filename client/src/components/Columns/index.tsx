import React from 'react';
import CreateColumns from './CreateColumns';
import classes from './Columns.modules.scss';

const Columns: React.FC = () => {
  return (
    <div>
       <CreateColumns />
    </div>
  );
};

export default Columns;
