import React from 'react';
import classes from './Main.module.scss';

const Main: React.FC = () => {
  return (
    <div>
      <h1 className={classes.title}>Home Page</h1>
    </div>
  );
};

export default Main;
